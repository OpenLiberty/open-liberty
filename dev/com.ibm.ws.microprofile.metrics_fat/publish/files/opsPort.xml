<server>
    <featureManager>
        <feature>jsp-2.3</feature>
        <feature>mpRestClient-1.2</feature>
        <feature>jaxrs-2.1</feature>
        <feature>localConnector-1.0</feature>
        <feature>mpMetrics-2.2</feature>
        <feature>monitor-1.0</feature>
    </featureManager>

    <include location="../fatTestPorts.xml" />

    <quickStartSecurity userName="theUser" userPassword="thePassword"/>
    <keyStore id="defaultKeyStore" password="Liberty"/>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="${bvt.prop.HTTP_default}"
                  httpsPort="${bvt.prop.HTTP_default.secure}" />
                  
    

    <httpEndpoint id="adminHttpEndpoint" httpPort="9082" httpsPort="9445" host="localhost" />

    <virtualHost id="admin_host"> 
        <hostAlias>localhost:9082</hostAlias>
        <hostAlias>localhost:9445</hostAlias>
    </virtualHost>

    <httpEndpoint id="opsHttpEndpoint" httpPort="9083" httpsPort="9446" host="localhost" />

    <virtualHost id="ops_host"> 
        <hostAlias>localhost:9083</hostAlias>
        <hostAlias>localhost:9446</hostAlias>
    </virtualHost>

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>
    <applicationMonitor updateTrigger="mbean"/>

    <!--Java2 security-->
    <javaPermission className="java.security.AllPermission"  name="*" actions="*" />

</server>
