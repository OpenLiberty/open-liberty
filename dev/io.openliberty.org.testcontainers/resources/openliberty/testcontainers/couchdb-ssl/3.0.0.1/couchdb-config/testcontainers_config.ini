; Disable clustering and sharding since this DB will only ever be used
; on small machines for a short period of time for tests
[cluster]
q = 1
n = 1

[ssl]
enable = true
cert_file = /etc/couchdb/cert/server.crt
key_file = /etc/couchdb/cert/server.key
tls_versions = ['tlsv1.2']