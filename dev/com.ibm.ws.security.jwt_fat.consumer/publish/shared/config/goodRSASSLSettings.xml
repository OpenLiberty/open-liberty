<server>

	<sslDefault sslRef="DefaultSSLSettings" />
	<ssl
		id="DefaultSSLSettings"
		keyStoreRef="rsa_key"
		trustStoreRef="rsa_trust"
		clientAuthenticationSupported="true" />
	<keyStore
		id="myKeyStore"
		password="Liberty"
		type="PKCS12"
		location="${server.config.dir}/commonBasicKeyStore.p12" />
	<keyStore
		id="myTrustStore"
		password="Liberty"
		type="PKCS12"
		location="${server.config.dir}/commonTrustStore.p12" />

	<keyStore
		id="configServerDefault"
		password="LibertyServer"
		type="PKCS12"
		location="${server.config.dir}/commonConfigServerDefault.p12" />

	<keyStore
		id="rsa_key"
		password="Liberty"
		type="PKCS12"
		location="${server.config.dir}/rsa_key.p12" />

	<keyStore
		id="rsa_trust"
		password="Liberty"
		type="PKCS12"
		location="${server.config.dir}/rsa_trust.p12" />

</server>        
