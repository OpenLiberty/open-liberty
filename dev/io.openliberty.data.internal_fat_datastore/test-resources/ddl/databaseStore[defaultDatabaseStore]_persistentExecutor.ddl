CREATE TABLE WLPPART (ID BIGINT IDENTITY NOT NULL, EXECUTOR VARCHAR(100) NOT NULL, EXPIRY BIGINT NOT NULL, HOSTNAME VARCHAR(100) NOT NULL, LSERVER VARCHAR(100) NOT NULL, STATES BIGINT NOT NULL, USERDIR VARCHAR(254) NOT NULL, PRIMARY KEY (ID));
CREATE TABLE WLPTASK (ID BIGINT IDENTITY NOT NULL, INAME VARCHAR(254) NOT NULL, LOADER VARCHAR(254) NOT NULL, MBITS SMALLINT NOT NULL, NEXTEXEC BIGINT NOT NULL, ORIGSUBMT BIGINT NOT NULL, OWNR VARCHAR(120) NOT NULL, PARTN BIGINT NOT NULL, PREVSCHED BIGINT, PREVSTART BIGINT, PREVSTOP BIGINT, RESLT LONGVARBINARY, RFAILS SMALLINT NOT NULL, STATES SMALLINT NOT NULL, TASKB LONGVARBINARY, TASKINFO LONGVARBINARY NOT NULL, TRIG LONGVARBINARY, TXTIMEOUT INTEGER NOT NULL, VERSION INTEGER NOT NULL, PRIMARY KEY (ID));
CREATE TABLE WLPPROP (ID VARCHAR(254) NOT NULL, VAL VARCHAR(254) NOT NULL, PRIMARY KEY (ID));
ALTER TABLE WLPPART ADD CONSTRAINT UNQ_WLPPART_0 UNIQUE (EXECUTOR, HOSTNAME, LSERVER, USERDIR);
